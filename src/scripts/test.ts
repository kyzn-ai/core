import { encodeMultilineString } from "~/utils/encode-multiline-string"

// Example usage:
const encodedString = encodeMultilineString(`Hello! You have 10 free messages to test the power of KYZN. Your journey starts now.
              
  To learn more, use the '@help' command.`)
console.log(encodedString)
