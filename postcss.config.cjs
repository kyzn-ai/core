/**
 * @file The configuration file for PostCSS, a tool used by Tailwind for transforming CSS with JavaScript plugins.
 * @author Riley Barabash <riley@rileybarabash.com>
 */

const config = {
    plugins: {
        tailwindcss: {},
        autoprefixer: {}
    }
}

module.exports = config
